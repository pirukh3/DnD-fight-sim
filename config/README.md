# Configuration

Configuration files for the simulator.

Examples:
- **Simulation settings** - Default parameters for combat simulations (e.g., number of rounds, variant rules)
- **Logging settings** - Log verbosity and output format
- **Environment-specific config** - Development vs. production settings
